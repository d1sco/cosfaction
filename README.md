# FactionDynamicEngine

The `FactionDynamicEngine` is a project focused on making the factions of multiplayer games more complicated and immersive.

## Cosfaction

The `Cosfaction` module is focused on using Taylor Series to model changes in relationships.